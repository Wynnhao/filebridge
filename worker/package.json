{
  "name": "filebridge-worker",
  "version": "0.1.0",
  "description": "FileBridge Server — Cloudflare Worker: MD/HTML upload, render & host",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "types": "wrangler types"
  },
  "dependencies": {
    "hono": "^4.3.7",
    "marked": "^12.0.0",
    "highlight.js": "^11.9.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240523.0",
    "typescript": "^5.4.5",
    "wrangler": "^3.57.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
